{
  "name": "@benchmarks/node-fetch",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "benchmark": "k6 run k6.js",
    "start:httpbin": "docker run --rm -p 50000:50000 -e PORT=50000 mccutchen/go-httpbin",
    "start:server": "tsx server.ts"
  },
  "devDependencies": {
    "@types/k6": "^1.2.0",
    "tsx": "^4.7.1"
  }
}
