---
"@whatwg-node/node-fetch": patch
---

Add TypeScript 6 compatibility: implement `values()` method and `[Symbol.asyncDispose]` on `PonyfillReadableStream` to match the updated `ReadableStream` interface
